void main() {
  bool valueFirst = 1;
  bool valuesecond = 0;
  print(valueFirst);
  print(valuesecond);
}


/*

- The program starts at `main()`.
- We create a boolean variable `valueFirst` and assign it the value `1`.
- We create another boolean variable `valueSecond` and assign it the value `0`.
- We try to assign `1` and `0` to boolean variables.
- This causes an error because Dart only allows `true` or `false` for booleans.
- The program will fail to run due to a type mismatch.

*/